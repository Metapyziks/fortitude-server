

			NOTES
	---------------------
	
	
			To Do
	---------------------


	Presentation
		Think of what to say
		Let Alice know what slides to put on prezi

	'Login/Signup'
		'Integrate errors neatly; asynchronious checking'
		// Should be two email fields
		// Activate account
		// Resend activation email
		// Forgot your password

	// Side bar
		// Change caches and recent news to display message when have none
		// Implement search function

	Map
		// Test with pins
		// Format pin colours
		Display cache on hover
		Center map on cache when searching by cache id
		// Link to cache details
		// Implement search ajax
		'Plan route to cache'

	// News
		// Add news items
		// Add dates and news titles

	// Help
		// Style FAQ
		// Add FAQ items
		// Split it into sections, create links to
		// Help intro screen

	// Home
		// Fill home screen

	// Account screen
		// Add forms and stuff
		// Change avatar
		// KING { 'Delete Account' }
		// Update password
		// Update message settings
		// Subsection / new page of all caches
			// Cache History
		// Subsection / new page of activity recording
		// Get date to display properly

	// Messages
		// Inbox
		// Send message	
		// Add link under user profile

	// Footer
		// Site map
		// Back to top
		// Add site map to footer
		// Credit stone wall

	// Levels

	Admins
		// Control panel
		// Cache list
			// !Cache location shown on map on hover or location links to view cache on map
			// Sort into tabs of cache type
		// User list
			// Link to user profile
			// Sort into tabs of user type
		Reports
		// Special Placement
			// Included in cache view
			// Create new
		// Moving delete caches
		// Updating place npc

		'Announcements'
		// Console log
		// Restart and update

		Filtering and searching cache list
		Filtering and searching user list

		// Styling of admin databases

	// User profile pages
		// Basic
		// Show all caches on a map
		Filter cache list to single cache from get request


	
	Handling cookies are:
	---------------------
	
	// Add new cookie:
	response.SetCookie (new Cookie ("cookie name", "cookie value" ));
	
	//Access cookie:
	request.Cookies["cookie name"].Value;
	
	//Overwrite cookie / update value:
	response.SetCookie (new Cookie ("cookie name", "cookie value two" ));
	